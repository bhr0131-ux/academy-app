# 미션팡 — Claude 작업 규칙

이 저장소로 작업하는 Claude는 아래 규칙을 반드시 따른다.

## 구조 원칙
1. Vite 다중 파일 구조 유지. App.jsx에 기능을 몰아넣지 않는다.
2. 새 화면·기능은 별도 컴포넌트 파일(src/components/)로 만든다. 파일당 30~80KB 목표.
3. 기존 화면을 수정할 일이 생기면, 가능한 범위에서 해당 화면을 App.jsx에서 컴포넌트로 분리하며 작업한다(한꺼번에 대규모 분리는 금지).
4. 이미지·에셋은 base64 내장 금지. public/assets/ 경로 방식만 사용.
5. 사용자가 준 캐릭터·장비 원화는 가공·탑재와 함께 art-src/에 원본 그대로 보관한다 (배포 미포함, 재가공용).
5. 상점 아이템·게임 수치는 데이터 파일(src/data/)에서만 관리. 화면 코드에 하드코딩 금지.

## 데이터 안전 원칙 (기존 사용자 보호)
6. 기존 저장 키(v6_* 등)와 save/load 로직은 절대 변경·삭제 금지.
7. 새 기능의 저장은 반드시 새 키로 추가. 기존 데이터 마이그레이션이 필요하면 읽기 폴백으로 처리.

## 작업 절차
8. 수정 전 기존 코드 구조 파악 → 수정 → 빌드 검증(vite build 또는 esbuild) 후 전달.
9. 기존 기능을 임의로 삭제·단순화하지 않는다. 삭제가 필요하면 먼저 사용자에게 확인.
10. 배포 전 DEV_MODE=false 확인을 사용자에게 상기시킨다.

## 배포 흐름
GitHub main 커밋 → Actions 자동 빌드 → Artifacts(missionpang-dist) → Capacitor 웹 폴더에 반영.
